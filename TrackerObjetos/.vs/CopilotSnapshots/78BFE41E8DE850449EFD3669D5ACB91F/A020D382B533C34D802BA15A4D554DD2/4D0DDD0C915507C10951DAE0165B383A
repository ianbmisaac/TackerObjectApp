using CommunityToolkit.Maui; // 1. Agrega este using arriba del todo
using TrackerObjetos.Platforms.Android;

namespace TrackerObjetos;

public static class MauiProgram
{
    public static MauiApp CreateMauiApp()
    {
        var builder = MauiApp.CreateBuilder();
        builder
            .UseMauiApp<App>()
            .UseMauiCommunityToolkit() // 2. AGREGA ESTA LÍNEA AQUÍ 👈
            .ConfigureMauiHandlers(handlers =>
            {
                handlers.AddHandler<TrackerObjetos.Controls.CameraPreview, TrackerObjetos.CameraPreviewHandler>();
            })
            .ConfigureFonts(fonts =>
            {
                fonts.AddFont("OpenSans-Regular.ttf", "OpenSansRegular");
                fonts.AddFont("OpenSans-Semibold.ttf", "OpenSansSemibold");
            });

        return builder.Build();
    }
}
